---
import type { FAQ } from "../../data/faqs";

interface Props {
  items: FAQ[];
}

const { items } = Astro.props;
---

<div class="faq-list">
  {items.map((faq) => (
    <details>
      <summary>{faq.question}</summary>
      <p>{faq.answer}</p>
    </details>
  ))}
</div>
