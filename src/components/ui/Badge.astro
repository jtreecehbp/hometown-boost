---
interface Props {
  variant?: "blue" | "orange" | "gold";
}

const { variant = "blue" } = Astro.props;
const classes = variant === "blue" ? "badge" : `badge badge--${variant}`;
---

<span class={classes}><slot /></span>
